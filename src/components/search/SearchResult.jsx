const SearchResult = ({searchProduct}) => {
  return (
    <div>
        {/* {searchProduct?.map(dt => (
            <div key={dt.id}>
                <img src={dt.image}/>
                <li >{dt.title}</li>
            </div>
        ))} */}
        
    </div>
  )
}

export default SearchResult;